<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
  </head>
  <body class="antialiased overflow-hidden <%= data.layout %> <%= data.page_class %>" data-controller="crisp-chat">
    <%= render "navbar", metadata: site.metadata, resource: resource %>

    <main class="min-h-[calc(100vh-10rem)] sm:min-h-[calc(100vh-16rem)] ">
      <%= yield %>
    </main>
  </body>
</html>

<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
  </head>
  <body class="<%= data.layout %> <%= data.page_class %>" data-controller="crisp-chat">
    <%= render Shared::Navbar.new(metadata: site.metadata, resource: resource) %>

    <main class="container prse min-h-[calc(100vh-10rem)] sm:min-h-[calc(100vh-16rem)]">
      <%= yield %>
    </main>
    <%= render "footer", metadata: site.metadata %>
  </body>
</html>
